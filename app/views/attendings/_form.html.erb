<%= form_with(model: attending) do |form| %>
  <% if attending.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attending.errors.count, "error") %> prohibited this attending from being saved:</h2>

      <ul>
        <% attending.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label "Histeory" %>
    <%= form.collection_select :attending_id, Attending.order(:name),:id,:name, include_blank: false %>  </div>

  <div class="form-group">
  <% Student.all.each do |student| %>
    <%= check_box_tag "attending[student_ids][]",student.id, @attending.student_ids.include?(student.id), id: dom_id(student),  :class => 'form-check-input' %>
    <%= label_tag dom_id(student), student.first_name, :class => 'form-check-label' %> <br>
  <% end %>
  </div>

    <%= form.submit "تسجيل حضور", :class => 'btn btn-primary' %>
  
<% end %>

