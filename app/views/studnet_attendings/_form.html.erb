<%= form_with(model: studnet_attending) do |form| %>
  <% if studnet_attending.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(studnet_attending.errors.count, "error") %> prohibited this studnet_attending from being saved:</h2>

      <ul>
        <% studnet_attending.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Histeory" %>
    <%= form.collection_select :attending_id, Attending.order(:name),:id,:name, include_blank: false %>  </div>

  <div class="form-group">
  <% Student.all.each do |student| %>
    <%= check_box_tag "studnet_attending[student_ids][]",student.id %>
    <%= label_tag dom_id(student), student.first_name, :class => 'form-check-label' %> <br>
  <% end %>
  </div>

    <%= form.submit "تسجيل حضور", :class => 'btn btn-primary' %>
<% end %>
      

