<%= form_with(model: studentclass) do |form| %>
  <% if studentclass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(studentclass.errors.count, "error") %> prohibited this studentclass from being saved:</h2>

      <ul>
        <% studentclass.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :students_id %>
    <%= form.text_field :students_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
