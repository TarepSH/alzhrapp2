<%= form_with(model: studnet_memorization) do |form| %>
  <% if studnet_memorization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(studnet_memorization.errors.count, "error") %> prohibited this studnet_memorization from being saved:</h2>

      <ul>
        <% studnet_memorization.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :memorization_id %>
    <%= form.text_field :memorization_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
