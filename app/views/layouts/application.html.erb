<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Alzhra App" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Alzhra App" %>">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%# stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%# javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    
    </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-success">
      <div class="container-fluid">  
        <a class="navbar-brand" href="#">Alzhra App</a>
          <div class=" navbar-collapse" id="navbarNav">
              <ul class="navbar-nav">
                <% if teacher_signed_in? %>
                  <li class="nav-item">
                      <%= link_to  "تسجيل حضور", attendings_path , class: "nav-link" %>
                  </li>
                  <li class="nav-item">
                  <%= link_to  "تسجيل تسميع", students_path , class: "nav-link" %>
                  </li>
                  <li class="nav-item">
                  <%= link_to  "قائمة الطلاب", students_path , class: "nav-link" %>
                  </li>
                    <li class="nav-item"><%= link_to "تسجيل خروج", destroy_teacher_session_path, :method => :delete, class: "nav-link" %></li>
                <% else %>
                    <li><%= link_to "تسجيل دخول", new_teacher_session_path , class: "nav-link" %></li>
                <% end %>
              </ul>
          </div>
      </div>    
    </nav>
    
    <% flash.each do |type, msg| %>
      <div class="alert alert-info">
        <%= msg %>
      </div>
    <% end %>
    <br>
    <main role="main" class="container">
    <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
<br>
     <!-- Footer -->
<footer class="page-footer font-small py-1 bg-success text-white-50 ">
    <div class="footer-copyright text-center py-3">Alzhra App
    </div>
  </footer>
  </body>
</html>
